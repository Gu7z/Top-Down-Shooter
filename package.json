{
  "name": "game",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "snowpack dev --polyfill-node",
    "build": "snowpack build --polyfill-node",
    "test": "c8 --all --reporter=text --reporter=lcov --include='src/**/*.js' --exclude='test/**' --exclude='public/**' --exclude='build/**' node --test --experimental-specifier-resolution=node $(find test -name '*.test.js' | sort)"
  },
  "devDependencies": {
    "@snowpack/plugin-dotenv": "^2.2.0",
    "c8": "^10.1.3",
    "snowpack": "^3.8.8"
  },
  "dependencies": {
    "crypto-js": "^4.2.0",
    "victor": "^1.1.0"
  },
  "type": "module"
}
